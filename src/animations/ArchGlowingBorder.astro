<div class="round-glowing-border">
  <slot />
</div>

<style>
  @property --glow-deg {
    syntax: "<angle>";
    inherits: true;
    initial-value: -90deg;
  }

  .round-glowing-border {
    --gradient-glow:
      rgb(207, 167, 52), rgb(226, 192, 92), rgb(245, 220, 150),
      rgb(255, 238, 190), rgb(235, 200, 120), rgb(215, 175, 80),
      rgb(207, 167, 52);

    display: inline-flex;
    justify-content: center;
    align-items: center;
    border-radius: 200px 200px 0 0;
    position: relative;
    isolation: isolate;

    background:
      linear-gradient(#faf6f1 0 0) padding-box,
      conic-gradient(from var(--glow-deg), var(--gradient-glow)) border-box;

    border: 4px solid transparent;
    transition: all 0.8s ease-in-out;
    animation: glow 10s infinite linear;
  }

  .round-glowing-border::before,
  .round-glowing-border::after {
    content: "";
    position: absolute;
    border-radius: 50%;
  }

  .round-glowing-border::before {
    background: #faf6f1;
    inset: 0;
    z-index: -1;
    filter: blur(var(--glow-size, 1rem));
  }

  .round-glowing-border::after {
    background: conic-gradient(from var(--glow-deg), var(--gradient-glow));
    inset: -1.5rem;
    z-index: -2;
    filter: blur(var(--glow-size, 1rem));
    opacity: var(--glow-intensity, 0.5);
  }

  @keyframes glow {
    100% {
      --glow-deg: 270deg;
    }
  }

  .round-glowing-border > * {
    border-radius: 200px 200px 0 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    position: relative;
    z-index: 1;
  }
</style>
